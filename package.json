{
  "name": "@walletwall/holder-wall",
  "version": "1.0.0",
  "description": "Holder Wall — read-only wallet-cohort treemap demo for vault-candidate discovery. Part of the WalletWall analytics suite.",
  "private": false,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "security:audit": "npm audit --audit-level=moderate",
    "check": "npm run test && npm run build && npm run security:audit"
  },
  "dependencies": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.1.0",
    "@testing-library/user-event": "^14.5.2",
    "@vitejs/plugin-react": "^6.0.3",
    "jsdom": "^29.1.1",
    "vite": "^8.1.5",
    "vitest": "^3.2.4"
  }
}
